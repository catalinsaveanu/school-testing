language: node_js
node_js:
  - "8.11.4"

before_script:
  - npm install -g firebase-tools

script:
  - npm run build

after_success:
  - firebase deploy --project "school-testing-97c98" --token $FIREBASE_TOKEN --non-interactive