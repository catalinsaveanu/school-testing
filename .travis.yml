language: node_js
sudo: false
node_js:
  - "8.11.4"
script:
  - npm run build
install:
  - npm install -g firebase-tools
  - npm install
after_succes:
  - firebase deploy — project "school-testing-97c98" — token $FIREBASE_TOKEN